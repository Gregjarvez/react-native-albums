
const container = {
  justifyContent: 'center',
  alignItems: 'center',
  flexDirection: 'row',
  backgroundColor: '#F8F8F8',
  height: 60,
  paddingTop: 15,
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.1,
  elevation: 2,
  position: 'relative'
};

const textStyle = {
  fontSize: 17,
  color: '#232323',
  fontWeight: 'bold',
  letterSpacing: 1.01
};

const HeaderText = {
  fontSize: 17
};

export { container, textStyle, HeaderText };
